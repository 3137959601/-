# C / C++

> 将原始5份复习笔记中的25个知识点按原顺序独立整理，不再按“复习01—05”分组；每点只补充必要边界，详细训练答案仍放在答案库。

## 1. volatile

- **重要度**：🔥🔥🔥
- **复习记录**：原笔记标注🟢；长期状态维持“学习中”，等待闭卷迁移题。

#### 核心结论

- `volatile`限制编译器对该对象访问的优化；对象可能被ISR、硬件或其他执行上下文异步改变。
- 常见场景：内存映射I/O（MMIO）寄存器、ISR与主程序共享的简单状态。
- `volatile != atomic != 线程安全`。
- `counter++`仍然是“读—改—写”复合操作，可能发生竞态。

#### 补充

- `volatile`不能代替互斥、临界区、原子操作或内存屏障。
- 访问硬件寄存器还要结合芯片手册判断读写副作用。W1C、只读、只写等寄存器不能机械套用普通读—改—写。
- 项目证据：DWT `CYCCNT`缺少`volatile`时，高优化下两次读取曾被合并，导致延时循环异常。

## 2. static

- **重要度**：🔥🔥🔥
- **复习记录**：原笔记标注🟢；待结合声明、定义和模块封装复测。

#### 核心结论

- 函数内`static`变量：块作用域、静态存储期，只初始化一次，程序运行期间一直存在。
- 文件作用域`static`变量：具有内部链接，只在当前翻译单元可见。
- `static`函数：具有内部链接，常用于隐藏模块内部实现。
- 生命周期、作用域和链接属性是三个不同概念。

#### 补充

- 头文件中定义`static`对象会让每个包含该头文件的`.c`文件各自得到一份副本，通常不是想要的共享状态。
- 需要放在头文件的小函数通常使用符合语言和编译器规则的`static inline`。

## 3. extern

- **重要度**：🔥🔥🔥
- **复习记录**：原笔记标注🟢；待闭卷解释跨文件声明与定义。

#### 核心结论

- `extern`通常表示“该对象或函数定义在别处”，当前声明一般不创建新的对象存储。
- 真正的存储实体位于定义处。
- 若定义被文件作用域`static`修饰，它具有内部链接，其他`.c`文件不能通过`extern`访问。

#### 补充

- 常见组织方式：头文件放`extern int g_value;`，某一个源文件放唯一的`int g_value = 0;`定义。
- `extern int x = 1;`带初始化器时仍是定义，不要仅凭`extern`关键字判断。

## 4. const

- **重要度**：🔥🔥🔥
- **复习记录**：原笔记标注“🟢 基本掌握”；待指针限定符变式。

#### 核心结论

- `const int *p`：不能通过`p`修改所指内容，`p`可以改指向。
- `int * const p`：`p`不能改指向，所指内容可以修改。
- `const int * const p`：两者都受限制。
- `const volatile uint32_t *reg`：软件不能通过该指针写寄存器，但寄存器值可能由硬件异步改变；指针本身仍可改指向。

#### 补充

- `const`表达的是通过当前访问路径的修改限制，不等于对象在物理上一定不可变。
- `const`对象放入`.rodata`是常见实现，但不是仅由C语法机械保证的固定段布局。

## 5. 数组与指针

- **重要度**：🔥🔥🔥
- **复习记录**：原笔记标注🟡；数组退化与函数形参`sizeof`需要复测。

#### 核心结论

- 数组不是指针，但数组表达式在很多场景会转换为指向首元素的指针。
- `int a[10]; sizeof(a)`得到整个数组的字节数。
- 函数形参`int arr[10]`会调整为`int *arr`。
- 因此函数内`sizeof(arr)`得到指针大小，不是数组大小，也不是首元素大小。

#### 典型Bug

```c
void clear_bad(int arr[10])
{
    memset(arr, 0, sizeof(arr));  // 通常只清除一个指针大小
}
```

#### 补充

- 函数若需要数组容量，应显式传入元素数量或字节数。
- 数组不发生常规退化的典型场景包括`sizeof(array)`和一元`&array`；二者得到的信息不同。
- 对应练习：[CPP-D-002](./题库/训练题库.md#cpp-d-002-数组字符串与内存函数综合排错)。

## 6. 野指针、悬空指针与内存泄漏

- **重要度**：🔥🔥🔥
- **复习记录**：原笔记标注🟢；生命周期仍与二级指针错题联合复测。

#### 核心结论

- 野指针：指针未初始化或保存了无法合法访问的未知地址。
- 悬空指针：原对象生命周期已经结束，但指针仍保存旧地址。
- 内存泄漏：动态对象仍占用内存，但程序丢失了能够正确释放它的有效引用。
- 返回普通局部变量地址会形成悬空指针。
- `static`局部变量在函数返回后仍存在，但多次调用共享同一个对象。

#### 补充

- `free(p); p = NULL;`只能避免当前变量继续悬空，不能自动清除其他指向同一内存的别名。
- 排查指针问题时依次问：它指向什么、对象是否还活着、容量多大、谁负责释放、是否存在别名。

## 7. 栈、堆与静态存储区

- **重要度**：🔥🔥🔥
- **复习记录**：原笔记标注“🟢 基本掌握”；段布局与任务栈需结合工程复测。

#### 核心结论

- 普通局部变量通常位于调用栈。
- `malloc/new`得到的动态对象通常位于堆。
- 已初始化的全局变量和`static`变量通常位于`.data`。
- 未初始化或零初始化的全局变量和`static`变量通常位于`.bss`。
- 只读全局常量常位于`.rodata`。

#### 补充

- 存储期是语言概念，栈、堆和段是常见实现布局；`static`不等于“一定在`.data`”。
- FreeRTOS每个任务通常有独立栈；任务栈常从FreeRTOS堆中一次性分配。栈高水位与尚未分配的`heap_free`不是同一个指标。

## 8. malloc、calloc、realloc与free

- **重要度**：🔥🔥🔥
- **复习记录**：原笔记对`realloc`标注“🟢 基本掌握”，但`malloc/free`实际编码暴露明显错误，整体按🟡处理。

#### 核心结论

- `malloc`申请一块字节内容未初始化的内存。
- `calloc`申请内存并把其字节清零。
- `realloc`可能原地调整，也可能搬迁到新地址。
- `realloc`失败返回`NULL`，原内存块仍然有效。
- 不要直接写`p = realloc(p, n);`，应先用临时指针接收。
- `realloc`成功后以返回的新指针为准，不再使用可能失效的旧指针。
- 使用完成后按所有权约定调用一次`free`。

#### 推荐写法

```c
int *tmp = realloc(p, new_count * sizeof *p);
if (tmp != NULL) {
    p = tmp;
}
```

#### 补充

- C中不需要强制转换`malloc`返回的`void *`；必须包含`<stdlib.h>`。
- 设计接口时明确失败后输出参数是否保持不变，以及成功后由谁释放。
- 对应练习：[CPP-C-003](./题库/训练题库.md#cpp-c-003-用二级指针创建并返回动态对象)。

## 9. memcpy与memmove

- **重要度**：🔥🔥🔥
- **复习记录**：原笔记标注🟡；需稳定判断重叠方向。

#### 核心结论

- `memcpy`要求源区域和目标区域不重叠；重叠时行为未定义。
- `memmove`支持重叠内存区域。
- 当`dst > src`且发生重叠时，常见安全策略是从后往前复制。
- 当`dst < src`且发生重叠时，常见安全策略是从前往后复制。

#### 补充

- “前向/后向复制”是解释`memmove`如何避免覆盖的常见实现思路，不要表述成C标准规定的唯一内部算法。
- 两个函数都只按字节数工作，不理解字符串终止符和对象所有权。

## 10. 结构体内存对齐

- **重要度**：🔥🔥🔥
- **复习记录**：原笔记标注“🟢 基本掌握”，但曾把所有成员都按最大对齐值计算，按🟡待复测。

#### 核心结论

- 每个成员放到满足其自身对齐要求的下一个地址。
- 所有成员放完后，结构体总大小通常补齐到最大成员对齐值的整数倍。
- 成员偏移量不等于成员对齐值。

#### 易错题

```c
struct Test2 {
    char  a;
    short b;
    int   c;
    char  d;
};
```

在常见`char/short/int`对齐为`1/2/4`的ABI下：

- `a`：offset 0
- `b`：offset 2
- `c`：offset 4
- `d`：offset 8
- `sizeof(struct Test2)`：12

#### 补充

- 具体布局受ABI、目标架构和`#pragma pack`等设置影响，回答具体数值前要说明假设。
- 结构体内存镜像不宜直接作为稳定通信报文；填充、ABI、大小端和版本变化都可能不一致。
- 对应练习：[CPP-K-004](./题库/训练题库.md#cpp-k-004-结构体对齐字节序与协议布局)。

## 11. sizeof与字符串

- **重要度**：🔥🔥🔥
- **复习记录**：原笔记标注🟢；待与数组形参综合复测。

#### 核心结论

- `sizeof(数组)`得到整个数组占用的字节数。
- `sizeof(指针)`得到指针本身的大小。
- 函数形参`int arr[10]`实际调整为`int *arr`。
- `strlen`从首字符统计到第一个`'\0'`，结果不包含`'\0'`。

#### 补充

- `sizeof`通常在编译期根据类型或对象计算，不会扫描字符串内容。
- 字符数组是否构成C字符串，取决于其可访问范围内是否存在终止符`'\0'`。

## 12. strcpy、strncpy与memcpy

- **重要度**：🔥🔥🔥
- **复习记录**：原笔记标注“🟢 基本掌握”；重点复测终止符与容量。

#### 核心结论

- `strcpy`复制到`'\0'`为止，不检查目标空间，空间不足会溢出。
- `strncpy(dst, src, n)`在源长度不小于`n`时不保证目标结尾有`'\0'`。
- `memcpy`只复制`n`个字节，不关心内容是否为字符串。

#### 易错题

```c
char dst[5];
strncpy(dst, "hello", sizeof(dst));
printf("%s", dst);
```

`dst`没有空间保存`'\0'`，`printf("%s")`可能越界读取。

一种基本修复方式：

```c
strncpy(dst, src, sizeof(dst) - 1);
dst[sizeof(dst) - 1] = '\0';
```

#### 补充

- 上述写法能保证终止，但仍要明确是否允许静默截断；更稳妥的接口应返回或检查源长度。
- `strncpy`在源较短时会用零字节填满剩余`n`字节，不能简单理解成“更安全的`strcpy`”。

## 13. 函数指针与回调

- **重要度**：🔥🔥
- **复习记录**：原笔记标注“🟢 基本掌握”；尚未独立完成接口编码。

#### 核心结论

- 函数指针保存与其类型兼容的函数地址。
- 回调是把函数注册给另一个模块，事件发生时由该模块调用。
- 嵌入式常见场景：UART/DMA完成通知、驱动事件、状态机分发。
- 核心价值：驱动层与业务层解耦，提高复用性和扩展性。
- 调用函数指针前应判空，并确保函数签名匹配。

#### 补充

- 常用`typedef`简化声明，例如`typedef void (*Callback)(int, void *);`。
- 注册回调时最好同时提供`void *context`，避免依赖全局变量。
- 必须约定回调运行在ISR还是任务上下文，以及数据有效期、能否阻塞和是否允许重入。
- 对应练习：[CPP-C-004](./题库/训练题库.md#cpp-c-004-设计带上下文的驱动回调接口)。

## 14. 宏与inline

- **重要度**：🔥🔥
- **复习记录**：原笔记标注“🟢 基本掌握”；需复测宏参数副作用。

#### 核心结论

- 宏在预处理阶段进行文本替换。
- `inline`仍保留函数语义和类型检查，只是允许编译器考虑内联。
- 宏参数可能被重复求值；内联函数的每个实参按函数调用语义求值一次。
- `inline`不保证编译器一定内联。
- 宏仍适合条件编译、平台配置和其他预处理器逻辑。

#### 易错题

```c
#define MAX(a, b) ((a) > (b) ? (a) : (b))
int x = 3, y = 4;
int z = MAX(x++, y++);
```

本次比较为假，因此：

- `x++`执行1次；
- `y++`执行2次；
- 不能简单说“两个参数都执行两次”。

#### 补充

- 不要把带副作用的表达式传给可能重复使用参数的宏。
- 即使编译器未实际内联，`inline`函数在可读性和类型安全上仍优于函数式宏。

## 15. 位运算与寄存器操作

- **重要度**：🔥🔥🔥
- **复习记录**：原笔记标注“🟢 基本掌握”，但清位时曾混淆`!`和`~`，按🟡待复测。

#### 核心操作

```c
reg |=  (1U << n);          // 第n位置1
reg &= ~(1U << n);          // 第n位清0
reg ^=  (1U << n);          // 第n位翻转
bit = (reg >> n) & 1U;      // 读取第n位
```

三位字段`bit[10:8]`更新：

```c
reg = (reg & ~(0x7U << 8)) | ((mode & 0x7U) << 8);
```

#### 易错点

- `!`是逻辑非，结果只有0或1。
- `~`是逐位取反。
- `reg &= !(1U << 5);`中的右侧结果为0，会把整个`reg`清零。

#### 补充

- 位移前应保证位号和操作数宽度合法，使用无符号常量避免符号扩展陷阱。
- Read-Modify-Write前必须核对寄存器位属性；W1C等寄存器可能需要直接写掩码。

## 16. 大小端

- **重要度**：🔥🔥🔥
- **复习记录**：原笔记标注🟢；待与协议编码联合复测。

#### 核心结论

`uint32_t v = 0x12345678;`

- 小端：低地址存低有效字节，内存从低到高为`78 56 34 12`。
- 大端：低地址存高有效字节，内存从低到高为`12 34 56 78`。
- 网络字节序通常为大端。
- `htons/htonl`：主机序转网络序；`ntohs/ntohl`：网络序转主机序。

#### 补充

- 字节序描述多字节对象的字节排列，不改变数值的数学意义。
- 线上协议应明确字段宽度和字节序，不能依赖本机结构体布局。

## 17. union

- **重要度**：🔥🔥
- **复习记录**：原笔记标注🟢；待与结构体布局联合复测。

#### 核心结论

`union`所有成员共享同一块存储。

```c
union Data {
    uint32_t value;
    uint8_t bytes[4];
};
```

在常见小端实现中，若`value = 0xAABBCCDD`，通常有：

```text
bytes[0] = DD
bytes[3] = AA
```

- `struct`成员各占自己的空间。
- `union`成员共享空间。
- `sizeof(union)`至少容纳最大成员，并满足最严格成员对齐。

#### 补充

- 应明确当前哪个成员保存有效值。
- 跨编译器、跨语言或线协议场景不要依赖未约定的联合体类型重解释；需要稳定字节表示时可逐字节编码或使用`memcpy`。

## 18. typedef与#define

- **重要度**：🔥🔥
- **复习记录**：原笔记标注“🟢 基本掌握”，但曾误读指针宏，按🟡待复测。

#### 核心结论

```c
typedef char *PSTR;
PSTR a, b;
```

`a`和`b`都是`char *`。

```c
#define PSTR2 char *
PSTR2 c, d;
```

文本展开为：

```c
char *c, d;
```

因此`c`是`char *`，`d`是`char`；`c`不是`char **`。

#### 补充

- `typedef`是真正的类型别名；`#define`只是文本替换。
- 复杂指针、函数指针和平台类型更适合用`typedef`提升可读性。

## 19. 一级指针与二级指针

- **重要度**：🔥🔥🔥
- **复习记录**：原笔记标注🟡；`malloc`错题需优先复测。

#### 核心结论

C函数参数按值传递：

```c
void f(int *p)
{
    p = NULL;  // 只修改形参副本
}
```

要修改调用者的指针变量，应传入该指针变量的地址：

```c
void f(int **pp)
{
    *pp = NULL;
}

int *p = NULL;
f(&p);
```

#### 典型错误

```c
void func(int **p)
{
    int x = 100;
    *p = &x;
}
```

虽然`*p`修改了外层指针，但`x`是普通局部变量；函数返回后`x`生命周期结束，外层指针变成悬空指针。

#### 正确方式一：指向调用者已有对象

```c
void point_to(int **pp, int *target)
{
    *pp = target;
}
```

#### 正确方式二：动态申请

```c
int allocate(int **out)
{
    if (out == NULL) {
        return -1;
    }

    int *tmp = malloc(sizeof *tmp);
    if (tmp == NULL) {
        return -1;
    }

    *tmp = 100;
    *out = tmp;
    return 0;
}
```

调用者成功取得所有权后负责`free`。

#### 本题错误点

- 正确调用是`func(&p)`，不是`func(p)`。
- 打印目标值是`printf("%d\n", *p)`，不是打印`&p`。
- 不能让外层指针指向函数局部变量。
- 动态申请后必须判空，并明确释放责任。

## 20. C程序的编译流程

- **重要度**：🔥🔥🔥
- **复习记录**：原笔记标注“🟢 基本掌握”；待根据报错判断阶段。

#### 核心流程

```text
.c
↓ 预处理
.i
↓ 编译
.s
↓ 汇编
.o
↓ 链接
ELF / 可执行文件
```

- 预处理：处理`#include`、`#define`、`#ifdef`等。
- 编译：把C/C++代码转换为汇编，并进行语法、类型等检查。
- 汇编：把汇编转换为机器指令，生成目标文件。
- 链接：合并多个目标文件和库，解析符号引用并完成重定位。

典型链接错误包括`undefined reference`和`multiple definition`。

## 21. 声明与定义

- **重要度**：🔥🔥🔥
- **复习记录**：原笔记标注“🟢 基本掌握”；待跨文件变式。

#### 核心结论

声明：

```c
extern int g_value;
```

定义：

```c
int g_value = 10;
```

推荐组织：

`config.h`

```c
extern int g_value;
```

`config.c`

```c
int g_value = 10;
```

#### 补充

- 声明告诉编译器名称和类型；定义提供对象实体或函数体。
- 普通全局变量定义不要直接放入会被多个`.c`包含的头文件。

## 22. include guard

- **重要度**：🔥🔥
- **复习记录**：原笔记标注“🟢 基本掌握”；待解释能力边界。

```c
#ifndef CONFIG_H
#define CONFIG_H

/* declarations */

#endif
```

#### 核心结论

- include guard防止同一个翻译单元重复包含同一个头文件。
- 它不能解决`a.c`和`b.c`都包含某头文件后，由头文件中的普通全局定义造成的跨翻译单元重复定义。

#### 补充

- `#pragma once`用途相近，但属于广泛支持的非标准扩展。
- include guard解决“重复包含”，`extern + 单一定义`解决“跨文件共享对象”。

## 23. .h与.c的职责

- **重要度**：🔥🔥🔥
- **复习记录**：原笔记标注“🟢 基本掌握”；待结合真实工程口述。

#### .h通常放

- 类型声明；
- 宏和常量；
- 函数声明；
- 必要的`extern`声明。

#### .c通常放

- 函数实现；
- 模块私有变量；
- `static`辅助函数。

原则：

> `.h`描述“别人怎么使用模块”，`.c`描述“模块内部怎么实现”。

若在头文件中写：

```c
static int count = 0;
```

每个包含它的翻译单元都会得到一份自己的`count`。

## 24. 头文件找不到：编译问题还是链接问题

- **重要度**：🔥🔥🔥
- **复习记录**：原笔记错题；当前🟡待复测。

```text
fatal error: uart.h: No such file or directory
```

这是预处理/编译阶段问题，不是链接问题。

常见原因：

- 头文件不在当前目录；
- 文件名或大小写不匹配；
- 没有配置include搜索路径；
- 生成文件尚未产生或路径未加入构建系统。

可通过正确相对路径或编译参数（例如`-I./driver`）解决。

快速判断：

| 报错示例 | 阶段 |
|---|---|
| `No such file or directory` | 预处理/编译 |
| `expected ';'`、类型不匹配 | 编译 |
| `undefined reference` | 链接 |
| `multiple definition` | 链接 |
| 构建成功后HardFault | 运行期 |

- 对应练习：[CPP-D-003](./题库/训练题库.md#cpp-d-003-判断预处理编译链接与运行期错误)。

## 25. 函数调用栈与栈溢出

- **重要度**：🔥🔥🔥
- **复习记录**：原笔记标注“🟢 基本掌握”；已有任务栈高水位工程证据，仍需闭卷解释。

#### 核心结论

函数调用时通常会使用栈保存：

- 局部变量或临时对象；
- 返回信息；
- 部分寄存器和调用上下文。

具体保存内容和布局取决于架构、ABI、编译器优化及函数实现。

典型风险：

```c
void process(void)
{
    uint8_t buf[8192];
}
```

如果所在FreeRTOS任务只有2 KiB栈，可能发生栈溢出。

#### 处理思路

1. 在有测量依据时增加任务栈。
2. 若生命周期和并发语义允许，把大缓冲区放入静态存储区。
3. 使用固定块内存池或受控堆，但评估碎片、失败路径和实时性。
4. 启用栈溢出检查，并在最坏功能路径下观察栈高水位。

#### 补充

- 每个FreeRTOS任务通常有独立栈；某个任务的局部大数组首先撑爆该任务自己的栈。
- 静态大缓冲区会引入共享、重入和长期占用问题，不能只看“不会爆栈”。
- 一次短时空载高水位不能证明最坏路径安全。

## 26. 指针数组、数组指针与指针运算

- **重要度**：🔥🔥🔥
- **复习记录**：已完成一轮复习；曾把同数组指针相减误认为字节数，当前🟡待间隔复测。

#### 核心结论

```c
int *p[10];      // 指针数组：10个元素，每个元素是int *
int (*q)[10];    // 数组指针：指向含10个int的数组
```

二维数组与数组指针：

```c
int a[3][5];
int (*row)[5] = a;
```

- `row + 1`跨过一整行，即`5 * sizeof(int)`字节。
- 同一个数组对象（或其尾后位置）内的两个指针相减，结果类型是`ptrdiff_t`，值为元素个数差，不是字节差。
- 尾后指针可以形成、比较和参与合法范围内的运算，但不能解引用。

#### 补充

- 只有指向同一数组对象及其尾后位置的指针才能进行有定义的相减；跨无关对象相减没有定义。
- 对应练习：[CPP-D-005](./题库/训练题库.md#cpp-d-005-指针数组数组指针与指针运算)。

## 27. void *与通用对象指针

- **重要度**：🔥🔥🔥
- **复习记录**：已完成一轮复习，待编码迁移。

#### 核心结论

- `void *`可以保存对象地址，但没有目标对象类型，不能直接解引用。
- 使用前必须转换或隐式转换为合适的对象指针类型。

```c
int a = 10;
void *p = &a;
*(int *)p = 20;
```

- C中`malloc`返回`void *`，可以直接赋给对象指针：`int *p = malloc(sizeof *p);`。
- 标准C不定义`void *`指针算术；需要按字节移动时先转换为`unsigned char *`或`char *`。

#### 补充

- 从`void *`取回的具体类型必须与实际对象兼容，否则可能违反对齐或别名规则。
- 函数回调的`void *context`只传递地址，不承担对象生命周期管理。

## 28. signed、unsigned、整数提升与溢出

- **重要度**：🔥🔥🔥
- **复习记录**：曾把`INT_MAX + 1`说成保证回绕到`INT_MIN`，当前🟡。

#### 核心结论

- 无符号整数运算按`2^N`取模，因此32位`unsigned int`的0减1得到`UINT_MAX`。
- 有符号整数溢出是未定义行为，不能保证回绕。
- signed/unsigned混合运算会执行通常算术转换，负的有符号值可能先转换成很大的无符号值。
- `uint8_t`等小整数类型参与表达式时通常先整数提升；`uint8_t d = 250 + 10;`最终转换回8位后得到4（在`uint8_t`存在且为8位时）。

#### 补充

- 边界判断应避免先发生溢出，例如加法前检查`a > INT_MAX - b`。
- 编译器可能基于“有符号溢出不会发生”进行优化，不能靠实机一次观察推断语言保证。
- 对应练习：[CPP-D-006](./题库/训练题库.md#cpp-d-006-signedunsigned与整数提升综合辨析)。

## 29. enum与状态机

- **重要度**：🔥🔥🔥
- **复习记录**：已完成基础复习，待状态转移编码。

```c
typedef enum {
    IDLE = 0,
    START,
    RUN = 10,
    STOP,
    ERROR
} State_t;
```

- `START = 1`、`STOP = 11`、`ERROR = 12`。
- `switch`漏写`break`会继续执行下一分支；若是有意fall-through，应显式注释或使用编译器认可的标记。
- 不要假设`enum`一定占1字节；跨平台协议、Flash布局或寄存器字段应使用明确宽度并显式转换。

#### 补充

- 工程状态机应列出状态、事件、守卫条件、动作和非法转移；`default`分支应提供安全恢复或错误记录。
- 中断和任务共同修改状态时，还必须明确同步与所有权。

## 30. 位域bit-field

- **重要度**：🔥🔥
- **复习记录**：已完成基础复习，待布局风险辨析。

```c
struct Status {
    unsigned int ready : 1;
    unsigned int error : 1;
    unsigned int mode  : 2;
};
```

- 位域的分配单元、位顺序、跨单元方式和对齐依赖实现与ABI。
- 不能只把位数相加就断言结构体大小，也不宜直接作为跨平台协议布局。

#### 补充

- 位域适合受同一编译器/ABI约束的本地状态表示；稳定报文或硬件寄存器映射通常优先使用定宽整数加掩码。
- 位域地址不能像普通成员一样随意取得。

## 31. #pragma pack与packed

- **重要度**：🔥🔥
- **复习记录**：已完成基础复习，待与对齐/协议综合复测。

```c
#pragma pack(push, 1)
struct Packet {
    uint8_t  type;
    uint32_t len;
    uint16_t crc;
};
#pragma pack(pop)
```

常见实现中`sizeof(struct Packet) == 7`，`len`偏移为1。

#### 核心边界

- packed可以减少或取消填充，但不能解决大小端、字段语义和版本兼容。
- 非对齐访问可能变慢，某些CPU会异常；读取packed多字节字段时可先`memcpy`到对齐的本地对象再使用。
- `#pragma pack`和`__attribute__((packed))`属于实现相关机制，必须核对编译器文档。

## 32. volatile、ISR与临界区

- **重要度**：🔥🔥🔥
- **复习记录**：概念已复习，仍需结合事件计数场景复测。

- `volatile`只约束编译器访问，不提供原子性、互斥或事件排队。
- `volatile int counter; counter++;`仍是读—改—写，Task与ISR交错时可能丢更新。
- 常见处理方式：平台原子操作、极短临界区、ISR-safe RTOS API、Queue、Semaphore或Task Notification。
- 单个布尔flag只能表达“当前有/无事件”，事件到达速度高于消费速度时，多次ISR可能被合并成一次。

#### 补充

- 临界区会增加中断延迟，应只保护最小共享状态，不能把长耗时工作放入临界区。
- 选择通信机制时先判断需要保存“状态、计数、值还是完整消息”。

## 33. Use-After-Free、Double Free与指针别名

- **重要度**：🔥🔥🔥
- **复习记录**：已完成基础复习；别名与释放责任需闭卷Debug。

```c
int *p = malloc(sizeof *p);
int *q = p;
free(p);
p = NULL;
*q = 20;          // Use-After-Free
```

- `p = NULL`只改变`p`，不会使别名`q`自动失效。
- 对同一动态对象重复`free`是Double Free，属于未定义行为。
- `free(NULL)`安全。

#### 补充

- 释放后不要读取、写入、比较对象内容或再次释放；必须从所有权设计上避免别名继续使用。
- C中可通过“单一所有者、借用不释放、统一销毁接口”降低风险。
- 对应练习：[CPP-D-007](./题库/训练题库.md#cpp-d-007-动态内存释放后的别名与重复释放)。

## 34. 数组越界与off-by-one

- **重要度**：🔥🔥🔥
- **复习记录**：已完成基础复习，待边界代码审查。

```c
int a[5];
for (int i = 0; i <= 5; ++i) {
    a[i] = i;
}
```

`i == 5`时已经越界。数组有效下标是`0`到`4`。

#### 补充

- 嵌入式越界可能破坏栈、返回信息、Task上下文、相邻缓冲区或控制变量，表现为HardFault、随机跑飞或延迟故障。
- 循环审查同时检查初值、终止条件、步长、容量单位以及`<`/`<=`。

## 35. 数组形参与sizeof（重复暴露错题）

- **重要度**：🔥🔥🔥
- **复习记录**：本知识点再次答错，随后即时复测答对；当前🟡，不能据此标记掌握。

```c
void clear(int arr[])
{
    memset(arr, 0, sizeof(arr));  // sizeof(arr)是指针大小
}
```

函数形参中的`int arr[]`、`int arr[100]`和`int *arr`都会调整为指针参数。

正确方式是显式传容量：

```c
void clear(int *arr, size_t n)
{
    memset(arr, 0, n * sizeof arr[0]);
}
```

- 对应练习：[CPP-D-008](./题库/训练题库.md#cpp-d-008-数组形参与sizeof重复错题)。

## 36. const volatile指针组合

- **重要度**：🔥🔥🔥
- **复习记录**：曾误认为`const volatile uint32_t *reg`可以通过`reg`写数据，反向题已即时纠正；当前🟡。

```c
const volatile uint32_t *reg;
```

- `reg`本身可以改指向。
- 不能通过`reg`修改所指数据。
- 数据可能由硬件异步变化，因此保留`volatile`访问。

常见组合：

```c
const volatile uint32_t * const ro_reg = ...; // 软件只读，地址固定
volatile uint32_t * const rw_reg = ...;       // 软件可读写，地址固定
```

#### 补充

- 先从标识符向外读声明，再分别判断“指针是否const”和“目标是否const/volatile”。
- 对应练习：[CPP-D-009](./题库/训练题库.md#cpp-d-009-const-volatile指针组合)。

## 37. 生命周期、static缓冲区与可重入性

- **重要度**：🔥🔥🔥
- **复习记录**：曾误认为两次调用会得到两块不同的static缓冲区；当前🟡。

```c
char *get_buffer(void)
{
    static char buf[16];
    return buf;
}
```

- `static`解决了函数返回后的生命周期问题，但所有调用共享同一块`buf`。
- 后一次调用可能覆盖前一次结果，多Task并发调用可能产生数据竞争。
- 可重入函数应避免共享可修改状态，或让调用者提供缓冲区和容量。

#### 补充

- Thread-safe不必然等同于可重入；信号/ISR重入还要求调用路径只使用可重入操作。
- 对应练习：[CPP-D-010](./题库/训练题库.md#cpp-d-010-static缓冲区与可重入性)。

## 38. 条件编译：#ifdef与#if

- **重要度**：🔥🔥
- **复习记录**：曾把`#define DEBUG 0`配合`#ifdef DEBUG`判断为不进入，随后即时纠正；当前🟡。

```c
#define DEBUG 0
```

- `#ifdef DEBUG`只判断宏是否已定义，因此条件成立。
- `#if DEBUG`计算宏替换后的常量表达式，`DEBUG`为0时条件不成立。
- 若宏可能未定义，可使用`#if defined(DEBUG) && DEBUG`。

#### 补充

- 配置宏应统一约定“是否定义”或“定义为0/1”的风格，避免混用。
- 对应练习：[CPP-D-011](./题库/训练题库.md#cpp-d-011-条件编译ifdef与if)。

## 39. 手写strlen

- **重要度**：🔥
- **复习记录**：🟢 即时完成；仍需在综合闭卷中复现。

```c
size_t my_strlen(const char *str)
{
    size_t i = 0;
    while (str[i] != '\0')
        ++i;
    return i;
}
```

- 遍历到`'\0'`为止，但返回值不包含终止符。
- 该简化实现要求`str`非空且指向一个在可访问范围内终止的C字符串。

## 40. 手写strcpy

- **重要度**：🔥
- **复习记录**：🟢 即时完成；仍需在综合闭卷中复现。

```c
char *my_strcpy(char *dst, const char *src)
{
    char *out = dst;
    while ((*dst++ = *src++) != '\0') {
    }
    return out;
}
```

- 必须连同`'\0'`一起复制，并返回原始`dst`地址。
- 该接口无法知道目标容量；调用者必须保证空间足够，源/目标也不能非法重叠。

## 41. 手写memcpy

- **重要度**：🔥
- **复习记录**：核心已完成，但曾遗漏`return dst;`；当前🟡待复测。

```c
void *my_memcpy(void *dst, const void *src, size_t n)
{
    unsigned char *d = dst;
    const unsigned char *s = src;
    void *out = dst;

    while (n--)
        *d++ = *s++;

    return out;
}
```

- 按字节复制`n`字节，不关心`'\0'`，不支持重叠区域。
- 保存原始目标地址后再移动工作指针，最后返回原始地址。

## 42. 简化版memmove

- **重要度**：🔥🔥
- **复习记录**：方向理解正确，但源指针转换和反向复制off-by-one出现错误；当前🟡。

#### 核心思路

- 目标起点位于源起点之前时，通常从前向后复制。
- 目标起点位于源起点之后且区域重叠时，从后向前复制。
- 反向复制先把指针移到尾后位置，再使用`*--d = *--s`。

```c
void *my_memmove(void *dst, const void *src, size_t n)
{
    unsigned char *d = dst;
    const unsigned char *s = src;
    void *out = dst;

    if (d < s) {
        while (n--)
            *d++ = *s++;
    } else if (d > s) {
        d += n;
        s += n;
        while (n--)
            *--d = *--s;
    }

    return out;
}
```

#### 本轮错误

- 错把指针转换写成对未初始化`s`的值转换；正确对象是`src`。
- `d += n`后指向尾后一字节，`*d--`会先越界解引用；必须使用`*--d`。

#### 补充

- 上述地址关系判断适合作为面试中的简化模型；严格可移植实现还要谨慎处理无关对象指针的关系比较，工程中优先使用标准库`memmove`。
- 对应练习：[CPP-C-005](./题库/训练题库.md#cpp-c-005-手写strlenstrcpymemcpy与memmove)。

---

<details>
<summary>后续C++专题（不计入本轮25个知识点）</summary>

- C++对象模型：待评估；后续覆盖类、继承、多态、虚函数、虚析构与对象布局。
- STL：学习中；后续覆盖`vector/string/map/unordered_map`、迭代器失效与复杂度选择。
- 多线程与同步：待评估；后续覆盖线程、互斥、条件变量、原子操作与竞态诊断。

</details>
