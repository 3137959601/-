# 算法与数据结构

> 本文件只保留知识主干、面试表达、练习入口和掌握状态；详细代码答案统一放在答案库。

## 1. 数组/字符串

- **状态**：待评估
- **重要度**：待评估
- **知识总结**：待学习后增量更新
- **必要练习**：待根据学习表现生成

## 2. 带哨兵头结点的单链表

- **状态**：🟡 基本理解但实现不稳定（2026-08-22）
- **重要度**：🔥🔥🔥
- **核心模型**：`head`是哨兵，`head->next`才是第一个有效节点；空表满足`head->next == NULL`。
- **头插顺序**：申请并判空 → 写数据 → `new_node->next = head->next` → `head->next = new_node`。
- **头删顺序**：保存`head->next` → 让`head->next`越过待删节点 → `free`旧节点。
- **尾插遍历**：使用会移动的工作指针`p`，循环条件是`while (p->next != NULL)`，最后执行`p->next = new_node`。
- **按值删除**：让`p`始终指向候选节点的前驱，命中后执行`p->next = tmp->next`并释放`tmp`。
- **本轮错误**：尾插循环错误地始终检查`head->next`；删除时写成`p = tmp->next`而没有修改链；`return -1`曾放在循环内部，导致只检查第一个候选节点。
- **补充**：分支返回前必须明确节点所有权；完整销毁时要逐节点释放，最后再处理哨兵是否由调用者持有。
- **必要练习**：[ALG-C-002](./题库/训练题库.md#alg-c-002-带哨兵单链表的插入删除与销毁)

## 3. 链式队列（带哨兵）

- **状态**：🟡 基本理解但首次出队有多处错误（2026-08-22）
- **重要度**：🔥🔥🔥
- **结构约定**：`front`指向哨兵；`rear`指向最后一个有效节点；空队列时两者都指向哨兵。
- **入队**：新节点`next = NULL`，先接到`q->rear->next`，再更新`q->rear = new_node`。
- **出队**：空队列拒绝；`tmp = q->front->next`；通过`*value = tmp->data`写回；让哨兵越过`tmp`；若删除的是最后一个有效节点，再令`rear = front`；最后释放`tmp`。
- **本轮错误**：漏写`q->rear`成员前缀；让`tmp`指向哨兵并误删哨兵；把`value = tmp->data`写成修改形参副本；删除最后节点后未恢复`rear`。
- **补充**：接口同时检查`q`和输出参数；初始化、清空与销毁操作必须约定哨兵由谁申请和释放。
- **必要练习**：[ALG-C-003](./题库/训练题库.md#alg-c-003-带哨兵链式队列入队与出队)

## 4. 哈希

- **状态**：待评估
- **重要度**：待评估
- **知识总结**：待学习后增量更新
- **必要练习**：待根据学习表现生成

## 5. 树

- **状态**：待评估
- **重要度**：待评估
- **知识总结**：待学习后增量更新
- **必要练习**：待根据学习表现生成

## 6. 排序

- **状态**：待评估
- **重要度**：待评估
- **知识总结**：待学习后增量更新
- **必要练习**：待根据学习表现生成

## 7. 二分

- **状态**：待评估
- **重要度**：待评估
- **知识总结**：待学习后增量更新
- **必要练习**：待根据学习表现生成

## 8. 双指针

- **状态**：待评估
- **重要度**：待评估
- **知识总结**：待学习后增量更新
- **必要练习**：待根据学习表现生成

## 9. 位运算

- **状态**：待评估
- **重要度**：待评估
- **知识总结**：待学习后增量更新
- **必要练习**：待根据学习表现生成

## 10. 环形缓冲区 Ring Buffer

- **状态**：🟡 基本理解，索引环回与成员访问曾出错（2026-08-22）
- **重要度**：🔥🔥🔥
- **方案**：采用“空一个位置”区分满与空。

```text
空：head == tail
满：(head + 1) % SIZE == tail
元素数：(head + SIZE - tail) % SIZE
最大容量：SIZE - 1
```

- `head`指向下一次写入位置，`tail`指向下一次读取位置。
- push先判满，再写`rb->buf[rb->head]`，最后令`head = (head + 1) % SIZE`。
- pop先判空，再通过输出参数返回`rb->buf[rb->tail]`，最后环回推进`tail`。
- **本轮错误**：写成`rb->buf[head]`漏掉成员前缀；使用`rb->head++`但未环回，最终可能越界。
- **补充**：`%`方案直观但容量必须非零；若容量为2的幂，可在满足无符号索引等前提下用掩码优化。ISR/Task并发还需单独分析SPSC、原子访问和内存顺序，不能只加`volatile`。
- **必要练习**：[ALG-C-001](./题库/训练题库.md#alg-c-001-空一格环形缓冲区的pushpop与计数)
